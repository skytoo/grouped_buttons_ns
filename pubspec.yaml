name: grouped_buttons_ns
description: Flutter package used to group radio and checbox buttons.
version: 1.0.1
homepage: https://github.com/syedassadullahshah/grouped_buttons_NS

environment:
  sdk: '>=3.3.1 <4.0.0'
  # flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
